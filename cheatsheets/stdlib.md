# Cheatsheet — Standard Library

> Fill as you complete Phase 4.

---

## Quick Reference

_Coming in Phase 4._

Topics to capture here:
- `fmt` — format verbs cheat card
- `strings` — most-used functions
- `strconv` — type conversions
- `io` — Reader/Writer/Closer interfaces
- `bufio` — Scanner, NewReader
- `os` — file I/O, env vars, args
- `encoding/json` — Marshal/Unmarshal/struct tags
- `net/http` — Handler, Client, ServeMux
- `time` — Duration arithmetic, formatting reference time
- `context` — WithCancel, WithTimeout patterns
- `log/slog` — structured logging

---

_Placeholder — fill at the end of Phase 4._
