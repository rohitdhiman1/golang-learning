# Cheatsheet — Concurrency

> Fill as you complete Phase 3.

---

## Quick Reference

_Coming in Phase 3._

Topics to capture here:
- Goroutine: `go fn()` — startup, lifecycle
- Channel creation: unbuffered vs buffered
- Send / receive syntax
- Directional channel types
- `select` patterns
- `close(ch)` and `range` over channel
- `sync.WaitGroup` idiom
- `sync.Mutex` lock/unlock pattern
- Worker pool template
- Fan-out / fan-in template
- Pipeline template
- Goroutine leak checklist

---

_Placeholder — fill at the end of Phase 3._
